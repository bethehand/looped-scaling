# 偏离记录（预注册 v1.0 第 6 节）

预注册正文 `01_预注册文档.md` 自 2026-09-23 16:40 CST 冻结后保持逐字节不变，可用 `shasum -a 256 -c 01_预注册文档.v1.0.sha256` 验证。
第 6 节"偏离记录"与附录 C"冻结的超参"的内容写在本文件，只追加、不修改。git 历史为每条记录提供时间戳。

格式：日期 / 偏离内容 / 原因 / 对结论的影响

## 记录

- 2026-09-23 / **实现细节澄清，非偏离** / 代码实现时对正文未明确之处的解释：(1) "冷却长度 = 分支点的 20%" 按 Hägele 协议实现为冷却占最终预算 D_j 的 20%，分支点在 0.8·D_j，冷却终点恰为 D_j；(2) 种子只改变初始化，数据顺序对所有任务相同；(3) 注入适配器默认随机初始化（备选恒等初始化作为稳定性后备，若启用记入偏离）；(4) 初始状态 s₀ = 0；(5) FLOP 计法：注意力项按全上下文 4·T·d 每层每 token 计入，输出头计入，输入嵌入查表计 0；(6) 截断反传实现为前 r−k 次循环在 no_grad 下执行，prelude 通过最后 k 次循环的注入获得梯度；(7) 检查点平均取冷却末尾 3 个相隔 25 步的评测点的损失均值 / 无 / 无。

- 2026-09-23 / **预算估计修正，非设计变更** / 配置生成器按正文定义精确计算后，主网格 111 次训练合计 66.8 单卡天（10M 2.4、20M 7.2、40M 24.7、80M 27.8、160M 尺子 4.8），学习率扫描 33 次约 4.5 单卡天；高于正文估计的 47，原因是 (1) N 按正文定义含输出头，各档实际 N 为 15.4M / 25.5M / 49.0M / 92.7M / 165M，预算随之增大；(2) FLOP 按正文口径计入全上下文注意力项，小宽度下占比 26% 到 50%；(3) 冷却 token 精确累加。按 4 卡估算约 3.5 周含开销。削减顺序不变 / 对结论无影响。

- 2026-09-24 / **训练数据改为 FineWeb-Edu sample-10BT；160M 尺子去掉 80N 预算点** / 作者此前已下载 sample-10BT（14 个文件，约 100 亿 GPT-2 token，16k 词表下约 110 亿），现网速约 1 MB/s，下载 100BT 的 25 个文件需一天以上。10BT 与 100BT 样本可能存在重叠文档，为保持单 epoch 不混用。文件 000–012 为训练流，013 保留为验证集。该流可覆盖除"160M 尺子 80N（132 亿 token）"外的全部预算，故 160M 尺子只取 5/10/20/40 倍 N 四个点 / 影响：160M 仅作尺子点，少一个数据量点对 β 的识别影响很小；其余单元不变。

- 2026-09-24 / **数据定稿；探索性代码验证集换源** / FineWeb-Edu 数据卡确认 "sample-10BT was sampled from sample-100BT"，10BT 是 100BT 的子集，二者混用会重复约一成文档，破坏单 epoch，故不再下载 100BT，上一条偏离即最终方案。the-stack-smol 为 gated 且为 JSON 格式，代码域探索验证集改用 codeparrot/github-code-clean 的首个 parquet 文件（未 gated，列名 code），键名 stack 改为 code；第二分布验证集取 SlimPajama-6B 的 validation 与 test 两个文件（各约 1000 万 token）以凑足约 2000 万 token / 仅影响探索性与稳健性验证集，不影响 φ 拟合。

- 2026-09-24 / **更正前两条记录中的数字，非设计变更** / 2026-09-23"预算估计修正"一条写的各档 N 有三处笔误，按代码精确计算为 15.4M / 26.8M / 50.2M / 92.7M / 179.6M（原文写成 25.5M、49.0M、165M）。相应地，2026-09-24"训练数据改为 sample-10BT"一条中 160M 尺子 80N 的需求应为 143.7 亿 token（原文 132 亿），40N 为 71.8 亿。manifest 与所有预算一直由代码按正确的 N 生成，只有记录文字有误。manifest 中最大的单次需求为 80M 尺子 80N 的 74.1 亿 token，10BT 训练流约 100 亿，结论不变 / 无影响。

- 2026-09-24 / **实现细节澄清：词表** / 预注册正文只写"16k 自训 BPE"。实现为：byte-level BPE，词表 16,384，含 1 个特殊符号 <|endoftext|> 作文档分隔；预分词采用 GPT-4 / Llama-3 的正则，数字按至多 3 位切分；用训练文件（不含留出的最后一个文件）的前 2×10⁹ 个字符训练，min_frequency=2；训练一次后冻结，所有任务共用 / 无影响。

- 2026-09-24 / **新增预先声明的次分析：不计输出头的参数口径** / 核对阅读笔记发现，iso-depth 的 N 不含 embedding 也不含输出头（其 Sec. 3.2 "total non-embedding parameters"），而本研究按 Porian / Parcae 把输出头计入 N。16k 词表下输出头占 N 的 34%（10M）到 12%（160M），两种口径得到的 φ 可能不同，小档尤甚。因此在任何数据产生之前声明：主分析仍按预注册口径（含输出头）；另用同一批结果按 iso-depth / Kaplan 口径（N_once 减去输出头 V·d）重做全部拟合，作为次分析，用于与 iso-depth 的 0.46 直接比较。H1、H2 在两种口径下分别判定；两者结论不一致时，该结论标为"依参数口径而异"。拟合脚本新增 `--n-def no_head` / 仅增加分析，不增加训练，不改变主结论的判定规则。

- 2026-09-24 / **评测协议：主指标改为冷却终点的损失，三点均值降为稳健性指标** / 代码审查发现原实现有误：冷却最后一步先做了一次尾部评测，收尾时又对同一组权重评测一次，"最后 3 个检查点均值"实际成了 (L₋₂₅ + 2·L₀)/3，而终点前 50 步那次评测算了却被丢弃。修正时进一步考虑到：冷却阶段损失单调下降，对末尾几个点取平均会系统性偏高，偏高量取决于冷却有多长，小档的短训练偏得最多，会让尺子随规模轻微弯曲；而验证集固定且足够大，终点损失的评测噪声本来就很小。因此在任何数据产生之前改为：主指标为冷却终点在全部验证集上的损失，与 Hägele 等的做法一致；预注册的三点均值（终点前 50 步、25 步与终点）保留为稳健性指标，只在主验证集与第二验证集上计算。两个指标都写入结果，都拟合，都报告。附带效果：每个分支的评测开销从 4 次全量评测降到 1 次全量加 2 次部分评测 / 主指标定义改变，发生在数据产生之前；两种指标都会报告，若结论不一致如实说明。

- 2026-09-24 / **实现错误修复：截断反传在 bf16 混合精度下收不到梯度** / GS01 吞吐测试中所有"中间块、r=8、截断"设置报错"checkpoint 重算值与前向不一致"。在本机装 PyTorch 2.6.0（与 GS01 相同）复现后确认根因：autocast 会缓存权重的 bf16 副本，截断时前 r−k 次循环在 no_grad 下运行，缓存下来的副本没有梯度通路，后面 k 次带梯度的循环复用它，于是循环核与注入层收不到任何梯度；开检查点时表现为重算报错，不开检查点时（如中间块 r=4 截断）则静默训练错误。PyTorch 2.14 已改变该缓存行为，故本机最初未复现。修复：no_grad 循环结束后调用 torch.clear_autocast_cache()。另补上 torch.utils.checkpoint 的显式导入（2.6 不会自动加载该子模块）。新增回归测试：在 CPU 与 CUDA 的 bf16 autocast 下，两种循环位置、有无检查点、有无截断，梯度与 fp32 的余弦相似度须大于 0.98；在 PyTorch 2.6 下去掉修复时该测试 4 项失败，加上修复后全部通过 / 发现与修复均在主网格任何训练之前；学习率扫描不含截断单元，不受影响。

- 2026-09-24 / **实现：全部任务启用逐层 torch.compile，数学不变** / GS01 实测扫描所用的 9 种设置提速 1.28 到 1.42 倍（中位 1.35），峰值显存下降。编译只作用于每个 Transformer 层内部的计算；循环、截断、清缓存、检查点仍在普通 Python 里执行。编译后的梯度与不编译的一致（余弦相似度大于 0.99），在 PyTorch 2.14 与 2.6 的 CPU 上以及 GS01 的 GPU 上测试。为绕开 PyTorch 2.6 编译器的一个 bug，关闭了编译器的模式匹配优化：其中一条只为 8 位量化权重准备的规则会把残差缩放 eps 误认为量化缩放系数，在整个图只有一个 eps 值时（普通模型、整栈循环）崩溃；逐点融合不受影响。学习率扫描与主网格的全部任务一律编译，结果行记录 compiled 与 PyTorch 版本 / 无影响；时间估计相应降为扫描约 0.8 天、主网格约 12 天。

- 2026-09-24 / **显存余量：两处减半微批次，纯系统参数** / 实测"中间块 r=4、无检查点"在 40M 与 80M 宽度下峰值显存 20.1 GB 与 21.5 GB，24 GB 卡余量不足。这两类单元的微批次由 20 减为 10（40M）、16 减为 8（80M），优化器的全局批次不变，梯度累积结果在数值误差内相同 / 无影响。

## 附录 C：冻结的超参（第 3 周学习率扫描后填写）

（η₀ 按五个宽度、β₂、每档实测吞吐、精确参数量）

### 数据与词表（2026-09-24 冻结）

| 项目 | 值 |
|---|---|
| 词表文件 | `data/tokenizer/bpe16k.json`，16,384，sha256 `9a1e0ed634536ed9eea6f472ccac755d2b2edb038f50f2be37ae15d85dba6895` |
| 训练流 | FineWeb-Edu sample-10BT 文件 000–012，共 10.41B token，固定顺序 |
| 主验证集 fwe_val | 文件 013 的前 50.0M token |
| 第二验证集 second_val | SlimPajama-6B 的 test 与 validation 文件，20.0M token |
| 探索性 finemath_val | finemath-4plus train-00000，20.0M token |
| 探索性 code_val | codeparrot/github-code-clean train-00000，20.0M token |

### 实测训练吞吐（GS01，RTX 4090，PyTorch 2.6.0，bf16，2026-09-24，单卡 token/s）

全部 11 种单元在修复梯度 bug 与调整微批次之后实测，机器可读版本见 `code/configs/throughput_measured.json`。每一行都通过了循环层梯度检查，峰值显存不超过 18.2 GB。

| 单元 | 320（10M） | 448（20M） | 640（40M） | 896（80M） |
|---|---|---|---|---|
| 普通 r=1 | 381,264 | 269,042 | 164,454 | 106,629 |
| 中间块 r=2 | 291,012 | 201,170 | 119,681 | 76,062 |
| 中间块 r=4 | 198,058 | 132,244 | 84,267 | 51,746 |
| 中间块 r=4 截断 | 256,675 | 174,446 | 112,069 | 69,327 |
| 中间块 r=8 | 95,430 | 63,863 | 35,718 | 22,089 |
| 中间块 r=8 截断 | 144,521 | 93,490 | 53,419 | 33,193 |
| 整栈 r=2 | 238,640 | 160,997 | 94,332 | 59,571 |
| 整栈 r=4 | 106,417 | 68,627 | 39,507 | 24,460 |
| 整栈 r=4 截断 | 153,860 | 109,917 | 61,797 | 38,543 |
| 整栈 r=8 | 55,596 | 36,110 | 20,266 | 12,496 |
| 整栈 r=8 截断 | 88,624 | 58,052 | 32,267 | 19,965 |

160M 普通模型（宽度 1280）为 63,876。按算法 FLOP 折算为 43 到 79 TFLOPS。

**时间表（按实测吞吐，含评测）**：学习率扫描 33 次约 3.4 单卡天，四卡约 1 天；主网格 111 次约 64.8 单卡天。主网格的排队顺序改为先跑 10M 一档（约 1 天内 11 种单元都跑过一遍），其余按预计耗时从长到短，四卡约 16.2 天，等于完美装箱的下限；原先按档从小到大排需 17.3 天。最长单次为 80M 整栈 r=8 截断，约 5.0 天。排队顺序只影响完成先后，不影响任何结果。

**开跑前检查 2 的执行方式**：预注册写的是"40M 上参数匹配时 r=4 优于 r=1；稠密尺子随宽度单调下降"。学习率扫描本身就包含五个宽度的稠密模型和 80M 的中间块 r=4，因此用扫描结果在 80M 上先做这项检查，40M 的对应结果在主网格开跑后几天内也会出来，届时补做一次。
