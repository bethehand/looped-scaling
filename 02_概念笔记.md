# 概念笔记（随讨论追加）

## 2026-09-23 循环与思维链的关系

**结论**：循环在理论上能承担思维链的一部分工作，但实际训出来的模型更像"每个 token 前多算几遍"的精修，不是一步一步的推理。

**依据**
- 理论上：Saunshi 等（ICLR 2025，arXiv 2502.17416）证明循环 T 次的模型存在权重可模拟 T 步思维链。但这是"存在"结果，不保证训练能找到；Merrill & Sabharwal（arXiv 2505.18948）把"能否学会使用循环层"列为开放问题。
- 实践上：对 Huginn 的分析（Lu 等 2507.02199；Blayney 等 2604.11791；Wang & Reid 2609.01924；Viakhirev 等 2608.18222）一致发现注意力模式在首次循环内定型、状态快速落入不动点或轨道、无清晰的推理阶段、中间状态弱可解码。Ouro 的循环可部分解码，但源于逐步监督的训练目标而非架构。
- 能力不对等：Xu & Sato（2509.25239）证明思维链独有随机解码带来的近似计数与采样能力；H. Zhang（2605.30757）证明压缩式循环缺少随长度增长的草稿纸。循环的优势在并行训练、不需思维链数据、不占上下文与 KV。

**论文用语**：不用"隐藏的思维链"（Huginn 作者与科普的说法，会被审稿人抓）。用"潜空间迭代计算"或"深度上的测试时计算"。

## 2026-09-23 "隐藏的思维链"的四种含义
1. 产品意义：思维链存在但不展示（o1 的做法）。
2. Anthropic 可解释性：一次前向传播内的多步隐状态计算（Tracing the thoughts of a large language model，2025）。循环深度是这种现象的架构化放大。
3. 思维链不忠实：可见推理不是真正的推理（Anthropic 2025；arXiv 2503.08679；2604.15726）。
4. 安全语境：模型故意隐藏或混淆推理（arXiv 2510.19851；Korbak 等可监控性论文点名 Coconut 与 Huginn）。

## 2026-09-23 循环与多采样（pass@k）的区别
- 多采样：独立随机尝试加验证器，抬高的是"至少一次答对"的覆盖率，不改变单次正确率 p。
- 循环：确定性地增加每个 token 的串行计算步骤，抬高的是 p 本身；无验证器、单一路径、到训练深度饱和甚至退化（Ouro T=8 掉分、LoopCoder-V2 R=3 崩塌、Parcae 的 L∞ 等于训练深度损失）。
- 两者相乘而非替代。混合方案已有：Huginn 并行采样器注入噪声、Latent Thinking Optimization 的潜空间奖励模型。

## 2026-09-23 φ 的含义
- φ∈[0,1]：循环核循环 r 次相当于把其参数乘以 r^φ。φ 接近 1 则用算力换显存划算；φ 低则同样算力应投给更大的稠密模型。φ=0.46（iso-depth）意味着循环省参数与显存、不省算力，是否值得取决于瓶颈是显存还是算力。
- 本研究测的是"训练时多循环一次值多少参数"，训练几次评测几次；"测试时多循环几次会怎样"是另一个问题（选题 #4）。

## 2026-09-23 拟合的可辨识性（合成数据测试的教训）
- 只有 4 档尺子（10M 到 80M，8 倍跨度）时，Chinchilla 形式里的 E、A、α 互相补偿：合成数据上 α 真值 0.30，仅用尺子拟合得到 0.23 到 0.27，且拟合损失比真值处还低，说明是可辨识性问题而非优化器问题。iso-depth 也报告 A、B "loosely identified"。
- 但 φ 对此不敏感：固定尺子参数后逐单元拟合，φ 真值 0.6 / 0.4 恢复为 0.62 / 0.40。联合拟合时 α 恢复到 0.29。
- 结论：(1) 保留 160M 尺子一点把跨度拉到 16 倍是必要的；(2) 论文中 α、E 的绝对值要给 bootstrap 区间并声明弱辨识，φ 才是主结论；(3) 拟合脚本对尺子行不分配 θ。

## 2026-09-26 与 H1 方向相反的发现：整栈循环在 r 小时，截断反而更好
- H1 猜"截断会让循环变差"，但至少在 20M 整栈 r=4 上，截断反而更好。主网格会测出这个现象在不同大小的模型上是否都存在，这会是论文里有意思的一点。
- 数据（20M，冷却终点的主考卷损失，1 倍学习率）：整栈 r=4 不截断 3.789，截断 3.645；普通模型 3.652；整栈 r=8 不截断 3.595，截断 3.589；整栈 r=2 不截断最好也只有 3.745（0.71 倍学习率）。中间块的截断与不截断几乎一样。
- 不是运气：换种子 43、44 重跑到第 400 步，不截断仍比截断落后 0.39 到 0.61；判定标准事先写定，见 `03_偏离记录.md` 2026-09-25 的两条。
- 一个待验证的猜测：整栈循环没有前后固定的层，梯度沿前几遍循环往回传的部分在训练初期起反作用；r 大时这部分被层层削弱，所以 r=8 不受影响。
- 主网格要看：10M 到 80M 各档是否都这样，差距随模型变大是缩小还是扩大。
