# 预注册文档：循环 Transformer 的循环等价指数 φ 及其设计因子

版本：**v1.0，已冻结**，2026-09-23。作者确认的决定：160M 循环单元延后、保留 160M 稠密尺子一点、新增 10M 档、截断只在 r=4/8、不加多 epoch 分支、预算约 47 单卡天。此后正文不再修改，任何变动只能追加到第 6 节"偏离记录"。
依据：`00_reading_notes/` 中 9 篇论文的笔记，每条决定后面标注来源编号 [01]–[09]。

---

## 1. 研究问题

**核心量**：循环等价指数 φ，定义为 L = E + A·(N_once + r^φ·N_rec)^(−α) + B·D^(−β) 中的 φ，即"循环核多循环一次，相当于增加多少独立参数"。iso-depth 在固定 r、全展开反传、r∈{1,2,4,8} 下测得 φ = 0.459，95% 区间 [0.41, 0.53] [01]。

- RQ1 函数形式：r^φ 形式与几何饱和形式 N_eff = N_once + N_rec·(1−ρ^r)/(1−ρ)，哪个在留一档外推上更准？
- RQ2 反传截断：只对最后 k=⌈r/2⌉ 次循环反传，φ 降低多少？在"等 token"和"等训练 FLOP"两种口径下分别是多少？（iso-depth 的 0.38 把两者混在一起 [01]）
- RQ3 循环位置：循环中间块（2+4+2）与循环整栈（8 层全循环），φ 是否不同？
- RQ4 交互：截断的效应是否依赖循环位置？
- RQ5 最优循环次数：在训练 FLOP 匹配与部署 FLOP 匹配两种口径下，拟合出的最优 r 各是多少？
- RQ6 规模趋势：因子效应在 10M→80M 内是扩大还是收窄？

**明确不做**：MoE；训练时采样 r；随机初始状态；多 epoch / 数据受限区间（φ 依赖训练区间 [05]，本研究只对单 epoch 下结论）；160M 及以上的循环单元（受算力限制延后，稠密尺子保留 160M 一点；日后追加时按本文档同一协议执行并记入偏离记录）；超连接；下游基准的结论性陈述。

## 2. 预注册假设与判定规则

| 假设 | 内容 | 支持的判定规则 |
|---|---|---|
| H1 | 截断反传降低 φ | φ_trunc < φ_full，且两者 95% bootstrap 区间不重叠；**在等 token 与等 FLOP 两种口径下都成立**才算支持。只在等 token 下成立，则结论为"效应来自 FLOP 再分配"。 |
| H2 | 循环中间块的 φ 高于循环整栈 | φ_mid > φ_whole，95% 区间不重叠；同时报告执行深度接近的原始损失对（中间块 r=8 对整栈 r=4；中间块 r=4 对整栈 r=2），若两种看法不一致则如实报告为"依口径而异"。 |
| H3 | φ 随 r 饱和，几何饱和形式优于 r^φ | 几何形式在两个方向的留一档外推（10–40M 预测 80M；20–80M 预测 10M）误差都更低，且 AIC 差值 > 10。 |
| H4 | 训练 FLOP 匹配下最优 r ≤ 2，部署 FLOP 匹配下最优 r 更大 | 由拟合公式计算，直接报告，不做假设检验。 |

**效应显著的最低门槛**：因子对 φ 的效应必须同时大于 bootstrap 区间半宽和 20M/40M 三种子测得的 φ 离散度，否则报告为"无法区分"。SMELT 的位置效应只有 0.006–0.008 nats 且单种子 [03]，本研究不接受单种子的位置结论。

## 3. 模型与网格

**共同设定**：8 个独立层；宽度 320 / 448 / 640 / 896（非嵌入参数约 10M / 19M / 39M / 77M，实现后填精确值），稠密尺子另加宽度 1280（约 157M）一点；RMSNorm pre-norm；SwiGLU；RoPE；无偏置；输入输出嵌入不绑定；词表 16k（自训 SentencePiece BPE）；序列长度 1024；bf16。

**参数计法** [08][09]：N = 非嵌入参数 + 输出头；输入嵌入单独记录不计入。N_once = prelude + coda + 输出头；N_rec = 循环核 + 注入适配器（2d² 线性层，随 iso-depth [01]）。整栈方案 N_once = 输出头。

**循环机制**：每次循环把 [s, e] 拼接后经线性层投回 d 维（e 为 prelude 输出，整栈方案为嵌入输出）；初始状态 s₀ = 0；训练与评测都用固定 r。

**稠密尺子**：r = 1 的普通 8 层模型，无注入适配器（与循环模型差 2d²，< 2% N，记录在案）。

**残差缩放与学习率** [06]：每个注意力/MLP 分支输出乘以 ε；循环核分支 ε = (1/r)·(L/12)^(−1/2)，非循环分支 ε = (L/12)^(−1/2)，L = 8。隐层学习率 η = η₀(d)·(L/12)^(−1/2)，**不随 r 变**；嵌入与输出头学习率 = η₀(d)。η₀(d) 在每个宽度的稠密尺子上单独扫描（5 点，2 倍间隔，10N token 预算），因为 [06] 只验证了跨 r 与跨层数的迁移，未验证跨宽度 [06][09]；在 80M、r=4 上做 ±2 倍交叉验证一次。

**优化器与调度** [06][07][09]：AdamW(0.9, 0.95)，权重衰减 0.1，梯度裁剪 1.0；warmup = 2N token；WSD：主干恒定学习率，在指定 token 处分出冷却分支，线性衰减到 0，**冷却长度 = 分支点的 20%**；每步 token 数 ∝ N^(2/3)：0.125M / 0.2M / 0.32M / 0.5M。β₂ 在 20M 扫描时同时测 0.99，若更优则 20M/40M 用 0.99。

**数据**：训练用 FineWeb-Edu sample-100BT（与 iso-depth、Parcae、Sparse Layers、残差缩放论文同源 [01][02][04][06]），固定分片顺序，所有任务同序，单 epoch；主验证集为 FineWeb-Edu held-out 50M token，固定不变；第二验证集取另一公开网络语料（SlimPajama 或 DCLM-baseline）held-out 20M token，用于检验 φ 是否依赖训练分布 [05][07]。**探索性验证集**（不参与 φ 拟合、不进入假设检验、结果单列为探索性）：数学文本 held-out 20M token（FineMath 或 OpenWebMath）与代码 held-out 20M token（the-stack），只用于报告"循环带来的损失下降是否随领域不同"，对应 SMELT 在代码上增益最大 [03] 与 Huginn 数学任务受益更多 [01 相关工作] 的观察。词表 16k 自训 BPE（小于文献常用的 32k–128k，为了让 10M/20M 档的输出头不主导参数量；损失绝对值因此不与其他论文直接可比，φ 为研究内相对量不受影响）。

**主网格**

| 维度 | 取值 |
|---|---|
| 档位 | 循环单元：10M、20M、40M、80M；稠密尺子：10M、20M、40M、80M、160M |
| r | 1（尺子）、2、4、8 |
| 循环位置 | 中间块 2+4+2；整栈 8 |
| 反传 | 全展开；截断 k = ⌈r/2⌉（只在 r=4、8 设置，r=2 时 k=1 已被 [01] 报告为系统性失配，不做） |
| 数据量分支（稠密尺子） | 每单位 N 5 / 10 / 20 / 40 / 80 token，5 点跨 16 倍以稳定 β [07][08][09] |
| 数据量分支（循环模型） | 每单位 N 10 / 20 / 40 token；截断任务另在等 FLOP 对应的 token 处加分支（主干延长至约 1.3 倍） |
| 种子 | 10M、20M、40M 全部单元各 3 个（42/43/44）；80M 主体 1 个，若时间允许在决定 H1/H2 的 4 个单元（中间块与整栈的 r=4 全展开/截断）加到 3 个；160M 尺子 1 个 |
| 10M 档的处理规则 | 若 10M 档在稠密尺子拟合中的残差超过其他档的 2 倍，则同时报告含与不含 10M 的拟合，以不含为主 |

**任务数**：每档尺子 1 + 中间块 5（r2、r4 全/截、r8 全/截）+ 整栈 5 = 11，四档共 44；160M 尺子 1；额外种子 66（10M/20M/40M 各 22）；学习率扫描约 32 → 共约 143 个任务，其中 111 个产出拟合数据。

**算力预算**（单卡 5×10¹³ FLOP/s 计，小宽度按更低利用率上调）：80M 约 18.5 单卡天，40M 约 5，20M 约 1.5，10M 约 0.6，160M 尺子约 4，三档种子约 14，学习率扫描约 3，合计约 47 单卡天，4 卡约 12 天纯跑，按 1.4 倍开销约 2.5 周。可选的 80M 四单元加种子另需约 11 单卡天，总计约 3.5 周。

**若时间不够，按此顺序削减**：① 放弃 80M 的可选加种子；② 去掉 80M 整栈 r=8 截断；③ 40M 种子减为 2。**日后若追加 160M 循环单元**，优先级为中间块 r=2/4/8 全展开 → 中间块截断 → 整栈 r=2/4 → 整栈 r=8（单次约 15 单卡天）。任何变动写入偏离记录。

## 4. 评测与拟合协议

**数据点**：每个冷却分支末尾的验证损失（最后 3 个检查点均值，nats/token）。每行记录：档位、N_once、N_rec、r、位置、反传、D、种子、损失、训练 FLOP（按执行层数计，含输出头与注意力项 [08][09]）、部署 FLOP/token、存储参数。

**拟合** [01][03][08]：对数空间 Huber 损失，δ = 10⁻³，LSE 参数化；L-BFGS-B，500 个起点，带边界；先在稠密尺子上拟合 E、A、α、B、β，再固定它们逐单元拟合 φ（主分析）；联合拟合作为次分析 [05]。置信区间：按（档位、r、预算）分块 bootstrap，1000 次。同时报告 φ 固定为 0 与 1 的受限拟合、去掉 r=8 的重拟合、半预算重拟合。**不以 R² 作为形式选择依据**（跨架构损失差只有约 0.1 nats [01]）。

**形式选择**：F1（r^φ）、F2（几何饱和 ρ）、F3（单元特定 φ 或 ρ，其余参数共享）。选择依据是留一档外推误差与 AIC，见 H3。

**稳健性**：种子离散度；第二验证集重拟合；去掉 r=8；等 token 与等 FLOP 两种截断口径。

## 5. 开跑前必须通过的检查

1. 截断反传梯度与手工推导一致；r=1 循环模型与稠密模型逐位一致（关闭适配器时）。
2. 40M 上参数匹配时 r=4 优于 r=1；稠密尺子损失随宽度单调下降。
3. FLOP 计数器与 PyTorch 实测 FLOP 一致（误差 < 5%）。
4. 五个宽度实测吞吐，重排时间表；若 80M 实测低于预估 30% 以上，启动削减顺序①。
5. 学习率扫描完成并冻结，写入本文档附录 C。

## 6. 偏离记录

（开跑后追加，格式：日期 / 偏离内容 / 原因 / 对结论的影响）

---

## 附录 A：每条决定的依据

| 决定 | 依据 |
|---|---|
| 固定 r、r∈{1,2,4,8}、prelude/coda = 2/2 | 与 iso-depth 主实验对齐以便比较 φ [01] |
| 截断 k=⌈r/2⌉ | iso-depth 的截断设置，其 0.38 由此得出 [01] |
| 截断分等 token / 等 FLOP | iso-depth 把省下的 FLOP 换成 1.3 倍 token，混淆了两个变量 [01] |
| 冷却 20%、线性到 0 | Hägele 图 5 在 20% 处收益饱和，5%–15% 不足 [07] |
| 5 个预算点跨 16 倍 | 3 点跨 4 倍拟不出 β [07][08][09] |
| N 含输出头、不含输入嵌入；FLOP 含输出头 | Porian 指出不计输出头 FLOP 使指数偏移 +0.13 [09]；Chinchilla 附录 F [08] |
| warmup = 2N token | Porian：N–4N 合适，固定长 warmup 使指数偏移 +0.10 [09] |
| batch ∝ N^(2/3)，学习率按宽度重扫 | Porian：不重调使指数偏移 +0.07–0.10 [09]；[06] 未验证跨宽度迁移 |
| ε = 1/r，学习率不随 r 变 | [06] 表 1/3/4 与图 5：1/r 下最优 LR 在 r∈{1,2,4,8} 恒定，1/√r 下漂移 |
| E、α、β 先在尺子上拟合再共享 | Chen–Wilson 的做法，避免 E 被循环单元拉动 [05] |
| Huber δ=10⁻³、500 起点、分块 bootstrap | iso-depth 与 Chinchilla 拟合协议 [01][08]；Besiroglu 复现指出求和而非平均 Huber [08] |
| 位置结论要求多种子并附执行深度匹配对 | SMELT 位置效应 0.006–0.008 nats、单种子、与宽度混淆 [03] |
| 不用 4 点无 E 幂律、不把嵌入计入 N | Sparse Layers 的两个方法问题 [04] |
| 只对单 epoch 下结论 | 多 epoch 下最优循环次数随算力增长，φ 不同 [05] |

## 附录 B：与文献的空白对照

iso-depth 与 Parcae 都未做整栈循环、未扫描截断、未报告种子、未公开代码 [01][02]；SMELT 与 Sparse Layers 无稠密循环的 φ、位置证据为单种子 [03][04]；Chen–Wilson 与残差缩放论文未测截断与位置 [05][06]。本研究的四个新增：整栈对比、截断的两口径分离、多种子、全部日志公开。

## 附录 C：冻结的超参（第 3 周填写）

（η₀ 按五个宽度、β₂、每档实测吞吐、精确参数量）
